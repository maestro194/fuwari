---
title: 8I0 - 21-01-2026
published: 2026-01-21
description: Hướng dẫn giải bài tập cho 8I0 - 21-01-2026
tags: [Editorial]
category: 8I0
draft: false
---

# **Bài 1: Thứ tự tấn công**

Ta có thể sử dụng cấu trúc hàng đợi ưu tiên, với lượt của các nhân vật được bắt đầu tại thới điểm $10000 / AV$, với mỗi 1 lượt lặp, cập nhật thông tin về $AV$ của lượt tiếp theo của nhân vật đang hành động

Độ phức tạp: $O(klog(n))$

<details>
<summary>Code mẫu</summary>

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false); cin.tie(NULL);

    int n, k, x;
    cin >> n >> k >> x;
    vector<double> a(n);
    for(int i = 0; i < n; i ++)
        cin >> a[i];
    priority_queue<pair<double, int>, vector<pair<double, int>>, greater<pair<double, int>>> pq;
    
    // tính AV bắt đầu cho từng nhân vật
    for(int i = 0; i < n; i ++) pq.push({10000 * 1.0 / a[i], i});
    int cnt = 0;
    for(int i = 1; i <= k; i ++) {
        double top = pq.top().first;
        int idx = pq.top().second;
        pq.pop();
        if (i == k) { // kết thúc lặp sau k lượt
          cout << idx;
          return 0;
        }
        if (idx == 1) { // xử lý riêng cho Seele
          cnt ++;
          if (cnt & 1) top += (10000 * 1.0 / (a[idx] + x));     // lượt có nâng cấp
          else top += (10000 * 1.0 / a[idx]);                   // lượt bình thường
        } else {
          top += (10000 * 1.0 / a[idx]);                        // các nhân vật khác
        }
        pq.push({top, idx});
    }
    return 0;
}

```
</details>

# **Bài 2: Tối ưu chiến kỹ**

<details>
<summary>Code mẫu</summary>

```cpp

```
</details>

# **Bài 3: Nguyên tố tương đương**

<details>
<summary>Code mẫu</summary>

```cpp

```
</details>
